<Window x:Class="GestorJRF.Ventanas.Genericas.VistaCompletaDosTablas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestorJRF.Ventanas.Genericas"
        mc:Ignorable="d"
        Title="VistaCompletaDosTablas" Height="583.42" Width="300" WindowState="Maximized">
    <Grid>
        <Grid HorizontalAlignment="Stretch" Height="517" Margin="10,10,10,0" VerticalAlignment="Top" Width="Auto" >
            <DataGrid x:Name="tabla1" SelectionMode="Single" IsReadOnly="True" ColumnWidth="*" CopyingRowClipboardContent="tabla1_CopyingRowClipboardContent" AutoGeneratingColumn="tabla1_AutoGeneratingColumn" DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" MouseDoubleClick="tabla1_MouseDoubleClick"/>
        </Grid>
        <Grid HorizontalAlignment="Center" Height="Auto" Margin="10,532,10,10" VerticalAlignment="Top" Width="Auto">
            <DataGrid x:Name="tabla2" SelectionMode="Single" IsReadOnly="True" ColumnWidth="*" CopyingRowClipboardContent="tabla2_CopyingRowClipboardContent" AutoGeneratingColumn="tabla2_AutoGeneratingColumn"  DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
        </Grid>
    </Grid>
</Window>
